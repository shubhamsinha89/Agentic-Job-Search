# LLM providers — install only what you use
openai>=1.30.0            # required for Groq + Ollama (OpenAI-compatible)
anthropic>=0.40.0         # optional — only if using LLM_PROVIDER=anthropic
# google-generativeai>=0.7  # optional — only if using LLM_PROVIDER=gemini

# Environment variables
python-dotenv>=0.21.0

# Resume parsing
python-docx>=1.1.0
pdfplumber>=0.11.0

# HTTP + search
requests>=2.32.0

# Config
PyYAML>=6.0

# Utilities
python-dateutil>=2.9.0